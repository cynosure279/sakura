#ifndef __CORE__H__
#define __CORE__H__

#include <vector>
#include <string>

namespace core {
    struct tag {
        std::string label; // 标签
        std::string color; // 颜色 hex 编码
    };
} // core

#endif  // !__CORE__H__